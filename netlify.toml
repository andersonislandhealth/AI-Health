[build]
  publish = "."

# Merge Sveltia CMS config (header + shared collections) before deploy.
[context.production]
  command = "python3 admin/merge-config.py production"

[context.cms]
  command = "python3 admin/merge-config.py staging"

# cms-2 is the active staging branch; overrides production merge when Netlify builds it.
[context.cms-2]
  command = "python3 admin/merge-config.py staging"

[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301

[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"
    X-Frame-Options = "DENY"
